<Accordion>
  <AccordionItem title="Workflow Won't Load">
    **Solution**: Verify the JSON format is valid ComfyUI export. Try re-exporting from ComfyUI if necessary. Ensure the file was exported using ComfyUI's "Save" option, not "Save (API Format)".
  </AccordionItem>
  
  <AccordionItem title="Import Fails with JSON Error">
    **Solution**: Check that your workflow JSON file is properly formatted. Open the file in a text editor to verify it's valid JSON. Re-export from ComfyUI if the file appears corrupted.
  </AccordionItem>
  
  <AccordionItem title="Workflow Runs Locally but Not in FlowScale">
    **Solution**: This is usually due to missing models or custom nodes. Check the error messages for specific missing dependencies and follow the model management workflow to resolve them.
  </AccordionItem>
  
  <AccordionItem title="Workflow Execution Fails">
    **Solution**: Switch to GPU mode if you're trying to run inference. Ensure all required models are properly uploaded and available in the correct folders. Check System Logs for detailed error messages.
  </AccordionItem>
</Accordion>
